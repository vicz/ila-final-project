pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                script {
                    sh """
                    set -e
                    ls -la
                    """
                }
            }
        }
    }
}
